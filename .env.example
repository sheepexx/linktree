# Public URL of the separately deployed commission API. Do not include a
# trailing slash. GitHub Pages builds read this from the repository variable
# with the same name.
NEXT_PUBLIC_COMMISSION_API_URL=https://commissions-api.example.com
